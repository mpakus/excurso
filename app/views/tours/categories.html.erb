<h1>Categories</h1>

<div class="row">
  <div class="col-lg-12">
    <% @categories.find_each do |category| %>
      <h3><%= link_to category.name, category_tour_path(category) %></h3>
    <% end %>
  </div>
</div>